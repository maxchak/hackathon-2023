import { LoginForm } from '@/features/Login'
import { LogoutButton } from '@/features/Logout'

export const LoginPage = () => {
  return (
    <>
      <LoginForm />
      <LogoutButton />
    </>
  )
}
